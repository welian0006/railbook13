<%= form_for(@clonet) do |f| %>
  <% if @clonet.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@clonet.errors.count, "error") %> prohibited this clonet from being saved:</h2>

      <ul>
      <% @clonet.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
